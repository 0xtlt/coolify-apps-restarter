# Coolify Instance Configuration
COOLIFY_TOKEN=your_coolify_api_token_here

# Webhook URLs (comma-separated)
# Get these from your Coolify application settings > Webhooks
WEBHOOK_URLS=https://your-coolify-instance.com/webhooks/app1,https://your-coolify-instance.com/webhooks/app2,https://your-coolify-instance.com/webhooks/app3

# Cron Schedule (using standard cron syntax)
# Examples:
# Every 6 hours: 0 */6 * * *
# Every day at 2 AM: 0 2 * * *
# Every Monday at 9 AM: 0 9 * * 1
# Every 30 minutes: */30 * * * *
CRON_SCHEDULE=0 */6 * * *

# Deploy on Start (trigger deployment immediately when the app starts)
# Default: false (off)
# Accepted values: true/false, on/off, yes/no, 1/0
DEPLOY_ON_START=false